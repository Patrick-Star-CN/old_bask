spring:
  application:
    name:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:
    username:
    password:
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    password:
    timeout: 100

server:
  port: 8080
  servlet:
    context-path:

mybatis-plus:
  global-config:
    db-config:
      table-prefix:
      id-type:

wechat:
  appid:
  secret: